/* Config del backend "Base de datos".
   api: ruta del API. En la web publicada (Vercel) es la función serverless
        del propio proyecto: '/api/bd' (las bases viven en el repo GitHub).
        Déjalo '' para modo local puro (sin backend, solo descarga generada).
   key: clave compartida con api/bd.js (env BD_KEY en Vercel o su default). */
window.NUVA_BD_CFG = { api: '/api/bd', key: 'NUVAOXI-BD-2607-kx94q' };
